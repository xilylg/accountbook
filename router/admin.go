package router

import (
	"github.com/gin-gonic/gin"
)

// 	cadmin "github.com/xilylg/accountbook/controller/admin"

func adminRouter(r *gin.Engine) {
	// 	admin := r.Group("/admin")
	// 	admin.GET("/login", cadmin.Login)
	// 	admin.Use(AdminAuth())
	// 	{
	// 		adminUser := admin.Group("/user")
	// 		{
	// 			adminUser.POST("/add", cadmin.AddUser)
	// 			adminUser.GET("/list", cadmin.UserList)
	// 		}
	// 	}
}
